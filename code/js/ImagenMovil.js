class ImagenMovil extends ImagenEditable {
  constructor(
		estado,
		posicion, 
		source, 
		tipo, 
		originalSize, 
		rotacion, 
		editor, 
		currentSize, 
		transparencia, 
		rol, 
		canvas) 
	{
    super(
			estado,
			posicion, 
			source, 
			tipo, 
			originalSize, 
			rotacion, 
			editor, 
			currentSize, 
			transparencia, 
			rol, 
			canvas); 
	
	}

  moveInEditor() {
    
  }
}